function forecast(arr) {
  // Only change code below this line
  let niceDay = arr.slice(2, 4);
  return niceDay;
}
// Only change code above this line
console.log(forecast(['cold', 'rainy', 'warm', 'sunny', 'cool', 'thunderstorms']));